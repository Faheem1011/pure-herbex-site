## 2024-05-18 - Prevent Unnecessary Array Operations in BotanicalGlossaryPage\n**Learning:** BotanicalGlossaryPage previously re-computed the filtered list of 124 ingredients, generating string methods on every render (and executing `searchTerm.toLowerCase()` per iteration). React state changes triggered expensive filters unneccessarily.\n**Action:** Wrapped `filteredIngredients` and `categories` logic using `useMemo` hooks, specifying correct dependency arrays, and hoisted `searchTerm.toLowerCase()` outside of the array filter loop to avoid repeating string methods within the loop, reducing compute overhead on simple render cycles.
